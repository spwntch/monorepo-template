describe('Light', () => {
  it('should TODO...', () => {
    const src = '/image.png';
    const image = { src };
    // const { baseElement } = render(<Light />);
    expect(image.src).toBe(src);
  });
});
